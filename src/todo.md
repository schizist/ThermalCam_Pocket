Need secure OTA, credentials in an env, add git ignroe. I can add my appropriate .env file to the project, but I don't want to share it. Also, I don't want to share the .pio directory which contains build artifacts and other local files.
OTA support for adjusting PID, changing mode, remote control, and remote veiwing. 
